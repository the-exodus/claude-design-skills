---
description: Smoke — the design-philosophy skill fires on an implementation request whose structure is still open.
tags: [smoke]
plugins: ["../../plugins/design"]
max_turns: 15
allowed_tools: [Read, Glob, Grep, Skill]
---

I need to add retry with exponential backoff to the HTTP client our payment service uses to call the card processor. There's no code to look at here, just tell me how you'd structure it.
