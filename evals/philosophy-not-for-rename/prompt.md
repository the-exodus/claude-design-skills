---
description: "design-philosophy does not load for a rename whose decision is already made."
tags: [philosophy]
plugins: ["../../plugins/design"]
max_turns: 20
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, Write, Edit]
---

Rename `fetch_all` to `fetch_stations` everywhere it appears. Nothing else changes.
